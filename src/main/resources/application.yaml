server:
  port: 8081
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/user_reg
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true


logging:
  level:
    org:
      mapstruct: debug


